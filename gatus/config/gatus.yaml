# ra-infrastructure Service Status - Gatus Configuration
# PRD: docs/prds/PRD-007-service-monitoring-dashboard.md
#
# Monitors: PostgreSQL, MySQL, pgAdmin, Cloudflare Tunnel endpoints
# Alerts: Email only (using SMTP from .env)
# Dashboard: http://localhost:8081

# =============================================================================
# STORAGE - SQLite for status history persistence
# =============================================================================
storage:
  type: sqlite
  path: /data/gatus.db

# =============================================================================
# WEB UI CONFIGURATION
# =============================================================================
web:
  address: 0.0.0.0
  port: 8080

ui:
  title: "ra-infrastructure Status"
  header: "Home Infrastructure Status"
  logo: ""
  link: ""

# =============================================================================
# ALERTING - Email Only
# =============================================================================
alerting:
  email:
    host: "${SMTP_HOST}"
    port: ${SMTP_PORT}
    from: "${SMTP_FROM}"
    username: "${SMTP_USERNAME}"
    password: "${SMTP_PASSWORD}"
    to: "${ALERT_EMAIL}"
    default-alert:
      enabled: true
      failure-threshold: 2
      success-threshold: 2
      send-on-resolved: true

# =============================================================================
# ENDPOINTS - Infrastructure Services
# =============================================================================
endpoints:

  # --------------------------------------------------------------------------
  # DATABASE SERVICES
  # --------------------------------------------------------------------------

  - name: PostgreSQL Database
    group: Databases
    url: "tcp://inventory-db:5432"
    interval: 30s
    conditions:
      - "[CONNECTED] == true"
    alerts:
      - type: email
    ui:
      hide-hostname: true

  - name: MySQL Database
    group: Databases
    url: "tcp://homeautomation-db:3306"
    interval: 30s
    conditions:
      - "[CONNECTED] == true"
    alerts:
      - type: email
    ui:
      hide-hostname: true

  # --------------------------------------------------------------------------
  # MANAGEMENT SERVICES
  # --------------------------------------------------------------------------

  - name: pgAdmin
    group: Management
    url: "http://inventory-pgadmin:80"
    interval: 60s
    conditions:
      - "[STATUS] == 200"
      - "[RESPONSE_TIME] < 5000"
    alerts:
      - type: email
    ui:
      hide-hostname: true

  # --------------------------------------------------------------------------
  # EXTERNAL ACCESS (Cloudflare Tunnel)
  # --------------------------------------------------------------------------

  - name: Snipe-IT (External)
    group: External Access
    url: "https://stuff.selfwize.com"
    interval: 60s
    conditions:
      - "[STATUS] == any(200, 302, 403)"
      - "[RESPONSE_TIME] < 10000"
      - "[CERTIFICATE_EXPIRATION] > 168h"
    alerts:
      - type: email
    ui:
      hide-hostname: true

  - name: Fasten Health (External)
    group: External Access
    url: "https://wellness.selfwize.com"
    interval: 60s
    conditions:
      - "[STATUS] == any(200, 302, 403)"
      - "[RESPONSE_TIME] < 10000"
      - "[CERTIFICATE_EXPIRATION] > 168h"
    alerts:
      - type: email
    ui:
      hide-hostname: true

  - name: Homarr Dashboard (External)
    group: External Access
    url: "https://dash.selfwize.com"
    interval: 60s
    conditions:
      - "[STATUS] == any(200, 302, 307)"
      - "[RESPONSE_TIME] < 10000"
      - "[CERTIFICATE_EXPIRATION] > 168h"
    alerts:
      - type: email
    ui:
      hide-hostname: true

  - name: Gatus Status (External)
    group: External Access
    url: "https://status.selfwize.com"
    interval: 60s
    conditions:
      - "[STATUS] == 200"
      - "[RESPONSE_TIME] < 10000"
      - "[CERTIFICATE_EXPIRATION] > 168h"
    alerts:
      - type: email
    ui:
      hide-hostname: true

  # --------------------------------------------------------------------------
  # INFRASTRUCTURE SERVICES
  # --------------------------------------------------------------------------

  - name: Traefik Reverse Proxy
    group: Infrastructure
    url: "http://host.docker.internal:8080/api/overview"
    interval: 30s
    conditions:
      - "[STATUS] == 200"
      - "[RESPONSE_TIME] < 2000"
    alerts:
      - type: email
    ui:
      hide-hostname: true

  # --------------------------------------------------------------------------
  # HOME AUTOMATION & SECURITY
  # --------------------------------------------------------------------------

  - name: Homeseer (External)
    group: External Access
    url: "https://home.selfwize.com"
    interval: 60s
    conditions:
      - "[STATUS] == any(200, 302, 401)"
      - "[RESPONSE_TIME] < 10000"
      - "[CERTIFICATE_EXPIRATION] > 168h"
    alerts:
      - type: email
    ui:
      hide-hostname: true

  - name: Blue Iris (External)
    group: External Access
    url: "https://cameras.selfwize.com"
    interval: 60s
    conditions:
      - "[STATUS] == any(200, 302, 401)"
      - "[RESPONSE_TIME] < 10000"
      - "[CERTIFICATE_EXPIRATION] > 168h"
    alerts:
      - type: email
    ui:
      hide-hostname: true

  - name: Homeseer (Internal)
    group: Home Automation
    url: "http://192.168.68.56:80"
    interval: 30s
    conditions:
      - "[STATUS] == any(200, 302, 401)"
      - "[RESPONSE_TIME] < 5000"
    alerts:
      - type: email
    ui:
      hide-hostname: true

  - name: Blue Iris (Internal)
    group: Home Automation
    url: "https://192.168.68.56:443"
    client:
      insecure: true
    interval: 30s
    conditions:
      - "[STATUS] == any(200, 302, 401)"
      - "[RESPONSE_TIME] < 5000"
    alerts:
      - type: email
    ui:
      hide-hostname: true

  # --------------------------------------------------------------------------
  # LIFE TRACKER SERVICES (ra-life-tracker)
  # --------------------------------------------------------------------------

  - name: Family Contacts (External)
    group: External Access
    url: "https://family.selfwize.com"
    interval: 60s
    conditions:
      - "[STATUS] == any(200, 302, 403)"
      - "[RESPONSE_TIME] < 10000"
      - "[CERTIFICATE_EXPIRATION] > 168h"
    alerts:
      - type: email
    ui:
      hide-hostname: true

  - name: Daily Event Log (External)
    group: External Access
    url: "https://events.selfwize.com"
    interval: 60s
    conditions:
      - "[STATUS] == any(200, 302, 403, 502)"
      - "[RESPONSE_TIME] < 10000"
      - "[CERTIFICATE_EXPIRATION] > 168h"
    alerts:
      - type: email
    ui:
      hide-hostname: true
