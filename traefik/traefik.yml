# Traefik Static Configuration - ra-infrastructure
# Listens on container port 80, mapped to host port 8070

api:
  dashboard: false

entryPoints:
  web:
    address: ":80"

providers:
  file:
    directory: /etc/traefik/dynamic
    watch: true

log:
  level: WARN
